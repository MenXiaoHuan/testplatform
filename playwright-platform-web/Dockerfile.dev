FROM node:20-alpine

# Development image used by docker compose; production builds use Dockerfile.
WORKDIR /workspace

EXPOSE 5173

# npm ci uses package-lock.json for reproducible installs in the dev container.
CMD ["sh", "-c", "npm ci && npm run dev -- --host 0.0.0.0"]
